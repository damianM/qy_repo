.partial
  .header.p_l_videos
  .body
    %table{:style => 'width:100%;'}
      - Video.latest.in_groups_of(4).each do |group|
        %tr
          - group.each do |video|
            %td
              = link_to((video.thumbnail ? (image_tag(video.thumbnail.public_filename)) : ("Miniaturka niedostępna")), video_path(video)) if video

    .button_silver_67{ :style => "margin-top:10px;float:right;" }
      %b= link_to "Wszystkie", videos_path
    .clr
.partial
  .header.workshops
  .body
    - form_for @workshop do |form|
      = render :partial => 'fields', :locals => { :form => form }

      .form_row
        = form.submit "Zapisz", :class => "submit"
      %span.required_field 
        * - pole wymagane

    = render :partial => 'assets/new', :locals => { :object => @workshop, :asset_type => "Image" }

    .clr

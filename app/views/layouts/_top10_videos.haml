.partial.partial_left
  .header.p_t_videos
  .body
   
    %ul.states
      - Video.top10.each_with_index do |video, i|
        %li
          %span= "#{i + 1}."
          = link_to truncate(video.title, 25, '...'), video_path(video)

    .button_silver_67{ :style => "float:right" }
      = link_to "Wszystkie", videos_path

    .clr
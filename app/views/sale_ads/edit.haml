.partial
  .header.ads
  .body
    - form_for @sale_ad do |form|
      = render :partial => 'fields', :locals => { :form => form }

      .form_row
        = form.submit "Zapisz", :class => "submit"
      %span.required_field 
        * - pole wymagane

    = render :partial => 'assets/new', :locals => { :object => @sale_ad, :asset_type => "Image" }

    .clr



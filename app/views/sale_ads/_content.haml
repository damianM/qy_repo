//= will_paginate @users, :renderer => 'RemoteLinkRenderer', :remote => remote_pagination_params, :previous_label => "&laquo;", :next_label => "&raquo;"

%table{:class => 'buy_ad tablesorter'}
  %thead
    %tr.tableHead2
      %th Tytuł
      %th Opis
      %th Email
      %th Telefon
  %tbody
    = render :partial => 'sale_ad', :collection => @sale_ads

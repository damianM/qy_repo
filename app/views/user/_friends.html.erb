<div class="partial">
  <div class="header p_myfriends"></div>
  <div class='body'>
    <% if user.friends.empty? %>
      Ten użytkownik nie ma jeszcze znajomych
    <% else %>
      <table class="friend_list">
        <% for user_row in user.friends %>
          <tr>
              <td>
                <div class="friend" style="margin-right:40px;">
                  <%= render :partial => "/userinfo", :locals =>
                    {:user => user, :protect=> false, :special => "joined"} %>
                </div>
              </td>
          </tr>
        <%end%>
      </table>
      <div class="button_silver_67" style="float:right ;">
        <%= link_to "Wszystkie",{:controller => "user", :action => "friends", :id => user.id} %>
      </div>
    <% end %>
  </div>
</div>

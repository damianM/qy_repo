.nav_gal
  = link_to "Galeria", user_galleries_path(@photo.gallery.user)

  - unless @gallery.nil?
    &raquo;
    = link_to @photo.gallery.name, user_gallery_path(@photo.gallery.user, @hoto.gallery)

  - unless @photo.nil?
    &raquo;
    - label = "zdjęcie" + ( @photo.description != "" ? " #{truncate(@photo.description, 40, '..')}" : "" )
    = link_to label, photo_path(@photo), :class => "nav_photo"				  
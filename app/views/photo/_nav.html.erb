<div class="nav_gal">
  <%= link_ "Galeria", "gallery", gallery.user.nil? ? "elist" : "list", gallery.user.nil? ? @gallery.event : gallery.user%>
  <% if !gallery.nil? %>
    <%= ">" %>
    <%= link_ gallery.name, "gallery", "index", gallery.id %>
  <% end %>
  <% if !photo.nil? %>
    <%= ">" %>
  <% type = photo.class == Video ? "Film" : "Zdjęcie" %>
    <%= link_to photo.description!="" ? "#{type}: #{cut photo.description,40}" : "#{type}:", {:controller => "photo", :action => "show", :id => photo.id}, :class => "nav_photo" %>
    
  <% end %>
</div>